# 🖥️ Shortcut Showdown (Tastenkürzel-Training)

**Kurs:** Cyber Security Analyst - Technical Foundation Basics | **Datum:** 19.06.2025

---

## Aufgabe

**Ziel:** Grundlegende macOS und Sublime Text Shortcuts für Navigation, Selektion und Bearbeitung ohne Maus üben.

---

## Lösung

### Umgebung
```
OS: macOS
Editor: Sublime Text
```

### Ausgangstext

```
The quick brown fox
jumps over the lazy dog.
Pack my box with
five dozen liquor jugs.
```

### Durchführung

**Schritt 1:** Neue Datei erstellen
```
Cmd+N
```

**Schritt 2:** Text kopieren und einfügen
```
Cmd+A (Alles auswählen)
Cmd+C (Kopieren)
Cmd+V (Einfügen)
```

**Schritt 3:** Cursor zum Wort "quick" bewegen
```
Cmd+Left Arrow    → Zeilenanfang
Option+Right Arrow → Nächstes Wort (vor "quick")
```

**Schritt 4:** Wort "quick" auswählen
```
Option+Shift+Right Arrow  ODER  Cmd+D
```

**Schritt 5:** Wort "quick" ausschneiden
```
Cmd+X
```

**Schritt 6:** Zum Zeilenende und "quick" einfügen
```
Cmd+Right Arrow → Zeilenende
Spacebar        → Leerzeichen
Cmd+V           → Einfügen
```
**Ergebnis:** `The brown fox quick`

**Schritt 7:** Zur zweiten Zeile, Zeilenanfang
```
Down Arrow
Cmd+Left Arrow
```

**Schritt 8:** Gesamte zweite Zeile auswählen
```
Cmd+L
```

**Schritt 9:** Zeile duplizieren
```
Cmd+Shift+D
```

**Schritt 10:** Zum Dateianfang
```
Cmd+Up Arrow
```

**Schritt 11:** Erste zwei Zeilen auswählen
```
Cmd+L (2x)  ODER  Shift+Down Arrow (2x)
```

**Schritt 12:** Auswahl kopieren
```
Cmd+C
```

**Schritt 13:** Zum Dateiende und neue Zeile
```
Cmd+Down Arrow
Enter
```

**Schritt 14:** Einfügen
```
Cmd+V
```

**Schritt 15:** Rückgängig machen
```
Cmd+Z
```

**Schritt 16:** Wiederherstellen
```
Cmd+Shift+Z
```

**Schritt 17:** Speichern als `shortcut_practice.txt`
```
Cmd+S → Dateiname eingeben → Enter
```

---

## Endergebnis

```
The brown fox quick
jumps over the lazy dog.
jumps over the lazy dog.
Pack my box with
five dozen liquor jugs.
The brown fox quick
jumps over the lazy dog.
```

---

## Shortcut-Übersicht

| Shortcut | Aktion |
|----------|--------|
| `Cmd+N` | Neue Datei |
| `Cmd+S` | Speichern |
| `Cmd+A` | Alles auswählen |
| `Cmd+C` | Kopieren |
| `Cmd+V` | Einfügen |
| `Cmd+X` | Ausschneiden |
| `Cmd+Z` | Rückgängig |
| `Cmd+Shift+Z` | Wiederherstellen |
| `Cmd+D` | Wort auswählen / nächstes gleiches |
| `Cmd+L` | Zeile auswählen |
| `Cmd+Shift+D` | Zeile duplizieren |
| `Cmd+Left/Right` | Zeilenanfang/-ende |
| `Cmd+Up/Down` | Dateianfang/-ende |
| `Option+Left/Right` | Wortweise springen |
| `Option+Shift+Arrow` | Wortweise auswählen |

---

## Notizen

- **Gelernt:** Navigation und Selektion ohne Maus
- **Tipp:** `Cmd` = Zeilen-/Dateiebene, `Option` = Wortebene
- **Wichtig:** `Shift` fügt Selektion hinzu

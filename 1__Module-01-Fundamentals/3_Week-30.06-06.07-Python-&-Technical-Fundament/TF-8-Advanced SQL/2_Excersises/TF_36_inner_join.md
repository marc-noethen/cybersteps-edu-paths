# 🖥️ Joining the Club

**Kurs:** Cyber Security Analyst - Technical Foundation Basics | **Datum:** 04.07.2025

---

## Aufgabe

**Ziel:** Daten aus mehreren Tabellen mit `INNER JOIN` kombinieren.

**Quelle:** [SQLBolt Lesson 6: Multi-table queries with JOINs](https://sqlbolt.com/lesson/select_queries_with_joins)

---

## Lösung

### Umgebung
```
Browser: Chrome / Firefox / Safari
Plattform: SQLBolt (Online)
```

### Durchführung

**Schritt 1:** Erklärungen auf der Seite lesen
- Verstehen, wie `INNER JOIN` funktioniert
- Syntax: `SELECT ... FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.id = tabelle2.id`

**Schritt 2:** Query-Aufgaben lösen

**Aufgabe 1:** [Beschreibung der Aufgabe]
```sql
-- Query hier einfügen
SELECT ...
FROM ...
INNER JOIN ... ON ...;
```

**Aufgabe 2:** [Beschreibung der Aufgabe]
```sql
-- Query hier einfügen
SELECT ...
FROM ...
INNER JOIN ... ON ...;
```

**Aufgabe 3:** [Beschreibung der Aufgabe]
```sql
-- Query hier einfügen
SELECT ...
FROM ...
INNER JOIN ... ON ...;
```

*(Weitere Aufgaben nach Bedarf hinzufügen)*

---

## Ergebnisse

| Aufgabe | Status |
|---------|--------|
| Task 1 | ✓ |
| Task 2 | ✓ |
| Task 3 | ✓ |
| ... | ✓ |

---

## Abgabe

📸 **Screenshot:** SQLBolt Lesson 6 mit allen sichtbaren Checkmarks (✓)

---

## Notizen

- **Gelernt:** `INNER JOIN` kombiniert Zeilen aus zwei Tabellen, wenn die Join-Bedingung erfüllt ist
- **Tipp:** Bei `INNER JOIN` werden nur Zeilen zurückgegeben, die in beiden Tabellen einen Match haben
- **Wichtig:** Die `ON`-Klausel definiert die Verknüpfungsbedingung zwischen den Tabellen
